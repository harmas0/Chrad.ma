async function e(e,t){if(!e||!t)return null;let n=`${e.lng},${e.lat};${t.lng},${t.lat}`;try{let e=await(await fetch(`https://router.project-osrm.org/route/v1/driving/${n}?overview=false`)).json();if(e.code===`Ok`&&e.routes?.[0]){let t=e.routes[0];return{distanceKm:Number((t.distance/1e3).toFixed(2)),durationMin:Math.ceil(t.duration/60)}}}catch(e){console.error(`calculateRouteDistance error:`,e)}return null}export{e as calculateRouteDistance};